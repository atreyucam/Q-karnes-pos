# Transitional UI Facade

`src/ui` ya no es la fuente de verdad del sistema visual.

Estado:

- mantiene compatibilidad para imports existentes
- reexporta desde `src/shared/ui`
- no debe recibir componentes nuevos
